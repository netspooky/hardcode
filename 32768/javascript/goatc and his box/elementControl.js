function setPosition(Something, x, y)
{
	Something.style["left"] = x + "px";
	Something.style["top"] = y + "px";
}

function setColor(something, r, g, b){
	something.style["color"] = "rgb(" + r + "," + g + "," + b + ")";
}