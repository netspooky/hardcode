Party: Simulaatio 7 (2016)
Entry name: Sevenance
By: joga
Compo: Newschool intro (size: 4864 B)

Instructions:
- Requires SDL2
- For Ubuntu:
    # Add universe repo first if needed (assuming 16.04):
    sudo add-apt-repository "deb http://us.archive.ubuntu.com/ubuntu/ xenial universe"
    sudo apt-get update
    sudo apt-get install libsdl2-2.0-0
- Binary says 1920x1080 but it should use the desktop resolution

Info/text for big screen:

64-bit Linux lo-filler. Aimed for 4k but it wasn't worth size optimizing. Mind the volume.

