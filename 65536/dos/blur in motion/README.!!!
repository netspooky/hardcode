
	Для муз.engin`a нужнa переменнaя BLASTER (например в
	AUTOEXEC.BAT : SET BLASTER=A220 I7 D1 H5 T4 )
				   port |  |  |  |
				       irq |  | тип SB
					  dma1|
					    dma2       
	( извините что так разжевываю, но может кто не знает ).

	Music играется на SB,SBPro,SB16.
	Tеоретически, eсть поддержка GUS, но она не проверена, может и 
	не работать (требует ULTRASND переменную), так что лучше под
	GUS не запускать. 

	Памяти требуется примерно 450 нижней, и 1.5Mb верхней.
	Идет как в DOS (raw,himem,vcpi), так и в WIN (dpmi).
